{#
/**
 * @file
 * Olivero's theme implementation for a page title.
 *
 * Available variables:
 * - title_attributes: HTML attributes for the page title element.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title: The page title, for use in the actual content.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 */
#}
{%
  set classes = [
    'title',
    'page-title',
    'display-5',         # Bootstrap 5 heading style
    'fw-bold',           # Emphasize title
    'mb-4'               # Add spacing below
  ]
%}

{{ title_prefix }}
{% if title|render|striptags|trim is not empty %}
  <h1{{ title_attributes
    .addClass(classes)
    .setAttribute('role', 'heading')
    .setAttribute('aria-level', '1')
    .setAttribute('aria-label', title|striptags|trim)
  }}>
    {{ title }}
  </h1>
{% endif %}
{{ title_suffix }}

